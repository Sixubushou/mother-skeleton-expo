import { Text, View } from "react-native";
import { Screen } from "../../components/Layout";

export default function SceneMatchingScreen() {
  return (
    <Screen>
      <View>
        <Text>Scene Matching (placeholder)</Text>
      </View>
    </Screen>
  );
}
